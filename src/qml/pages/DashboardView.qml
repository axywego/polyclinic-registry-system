import QtQuick
import QtQuick.Layouts
import QtQuick.Controls

Rectangle {
    Text {
        anchors.fill: parent
        anchors.margins: 20
        text: "в будущем тут чет будет"
        font.pixelSize: 32
    }
}